#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
mvn -q -f "$SCRIPT_DIR/pom.xml" package -DskipTests
exec java -jar "$SCRIPT_DIR/target/cli-task-tracker-1.0.0.jar" "$@"
